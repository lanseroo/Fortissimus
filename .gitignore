# Secrets — keep local, never commit (per project convention)
gas/Config.js
**/Config.js

# Node / clasp local
node_modules/
npm-debug.log*

# Design IP — repo is public, keep rules/design notes local
docs/
